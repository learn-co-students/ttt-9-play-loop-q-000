#!/usr/bin/env ruby

require_relative "../lib/play"

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "Welcome to Tic Tac Toe!"
display_board(board)

def play(board)
  counter = board.count
  until counter == 0
    turn(board)
    counter -= 1
  end
end

play(board)
